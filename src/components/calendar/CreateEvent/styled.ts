import { styled } from '@mui/material';
import { Input } from 'react-ui-expert';

export const Field = styled(Input)`
  outline: none;
  font-size: 1.5rem;
  padding: 5px;
  background: transparent;
  color: lightgrey;
  border: none;
  border-bottom: 0.5px solid white;
  border-radius: 4px;
`;
